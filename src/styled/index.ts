import styled from '@emotion/styled';

import { Button } from '@mui/material';

export const RoundedButton = styled(Button)`
  border-radius: 3rem;
`;

export const SemiRoundedButton = styled(Button)`
  border-radius: 0.75rem;
`;
